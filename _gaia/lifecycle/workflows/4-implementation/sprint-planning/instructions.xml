<workflow name="sprint-planning">
<critical>
  <mandate>Use sprint state machine: backlog, validating, ready-for-dev, in-progress, invalid, review, done</mandate>
  <mandate>Support multi-sprint via sprint_id tracking</mandate>
  <mandate>Only stories with status 'ready-for-dev' are selectable for a sprint</mandate>
</critical>
<step n="1" title="Load Epics, Stories, Action Items, and Previous Retro">
  <!-- E19-S28 AC3 / FR-315: parse optional --skip-gap-prompt flag and bind to local variable -->
  <action>FLAG PARSING (E19-S28 AC3): Inspect the invocation arguments passed to /gaia-sprint-plan. If the flag `--skip-gap-prompt` is present, set the local variable `skip_gap_prompt = true`. Otherwise set `skip_gap_prompt = false` (the default declared in workflow.yaml). This variable is consulted by Step 3b to suppress the FR-237 gap analysis tip. The flag is scoped to sprint-planning only and does NOT affect the independent invocation path of /gaia-fill-test-gaps.</action>
  <action>Read {implementation_artifacts}/action-items.yaml if it exists. Check for HIGH priority items open for 2+ sprints (escalation_count >= 2 AND priority == "high" AND status == "open").
    If any found: HALT — "Cannot start sprint planning. {count} HIGH priority action item(s) have been open for {escalation_count}+ sprints:
    {for each: A-{NNN}: {title} (open since {source_sprint}) → Run: /gaia-action-items A-{NNN}}
    Resolve each item using the command shown above before re-running sprint planning."
    For MEDIUM/LOW open items: display warning but do NOT halt: "Note: {count} action items still open (non-blocking)."</action>
  <action>Read {planning_artifacts}/epics-and-stories.md</action>
  <action>Parse all stories with their priorities, sizes, dependencies</action>
  <action>Scan {implementation_artifacts}/ for individual story files matching {story_key}-*.md. For each file found, read its frontmatter Status field.</action>
  <action>Classify stories into selectable and non-selectable:
    - SELECTABLE: stories with individual files AND status = 'ready-for-dev'
    - NOT SELECTABLE: stories without individual files → "Story {key} has no individual file — run /gaia-create-story {key} first."
    - NOT SELECTABLE: stories with files but status ≠ 'ready-for-dev' → "Story {key} is in '{status}' status — must be 'ready-for-dev' to be selectable. Run /gaia-validate-story {key} first."
  </action>
  <action>Display the classification to the user: selectable stories table (Key | Title | Priority | Size | Risk | Status) and non-selectable stories with reasons.</action>
  <action>Priority flag scan: scan all selectable story files for priority_flag: "next-sprint" in their YAML frontmatter. If any flagged stories are found, display them with a visual indicator: "FLAGGED FOR NEXT SPRINT: Story {key} — {title} (priority_flag: next-sprint). This story was flagged by the add-feature workflow for priority inclusion." List all flagged stories before story selection begins so the scrum master is aware.</action>
  <action>Load most recent retro-{sprint_id}.md from {implementation_artifacts}/ if available</action>
  <action>If retro found: extract open action items — carry forward as sprint constraints or tasks. Present to user: "Previous retro action items to carry forward:" with status of each.</action>
</step>
<step n="2" title="Optional: Memory Hygiene Check" optional="true">
  <action>Check if any sidecar directories under {memory_path}/*-sidecar/ contain content beyond their template header</action>
  <check if="any sidecars have content">
    <ask>Agent memory sidecars contain decisions from previous sessions. Would you like to run memory hygiene? This spawns a subagent in a separate context. Recommended if architecture or requirement changes occurred since last sprint. (yes / skip)</ask>
    <action>If yes: spawn a subagent using the Agent tool: "Load {project-root}/_gaia/core/engine/workflow.xml, then process {project-root}/_gaia/lifecycle/workflows/anytime/memory-hygiene/workflow.yaml as workflow-config. Run in YOLO mode. Follow the workflow engine instructions EXACTLY."</action>
    <action>If skip: memory hygiene can be run anytime with /gaia-memory-hygiene</action>
  </check>
  <check if="no sidecars have content">
    <ask>No agent memory sidecar data found. Would you like to run memory hygiene anyway? This is typically skipped for first sprints. (yes / skip)</ask>
    <action>If yes: spawn subagent as above</action>
    <action>If skip: continue to next step</action>
  </check>
</step>
<step n="3" title="Sprint Scoping">
  <ask>Sprint duration (1 week / 2 weeks / custom)?</ask>
  <ask>Team velocity estimate (story points)?</ask>
  <ask>Sprint number (for multi-sprint tracking)?</ask>
</step>
<step n="3b" title="Gap Analysis Tip">
  <critical>
    <mandate>This step implements FR-237 (E19-S18). It surfaces a gap-analysis reminder to the scrum master after the velocity check and before story selection so that testing gaps are visible when prioritizing stories. The tip is advisory only — it never blocks sprint planning.</mandate>
  </critical>
  <action>SKIP FLAG CHECK (AC4 / E19-S28 AC3): Consult the local variable `skip_gap_prompt` bound in Step 1 from the `--skip-gap-prompt` flag parsed per workflow.yaml flag declaration. If `skip_gap_prompt == true`, skip this step entirely — emit no prompt, no file check, no wait. Proceed directly to Step 4 (Story Selection). This gives the scrum master an explicit opt-out for teams that do not use gap analysis. The flag does NOT affect the independent invocation path of /gaia-fill-test-gaps — it is scoped solely to suppressing this sprint-planning tip.</action>
  <action>RESOLVE SPRINT START DATE: Derive {sprint_start_date} as the ISO date (YYYY-MM-DD) the current sprint is starting. Prefer the value from {implementation_artifacts}/sprint-status.yaml if it exists (field: started), otherwise fall back to today's date. This is the date used to locate the gap-analysis file for the current sprint.</action>
  <action>GAP ANALYSIS FILE CHECK (AC2): Check whether {test_artifacts}/test-gap-analysis-{sprint_start_date}.md exists (this resolves to docs/test-artifacts/test-gap-analysis-{sprint_start_date}.md under the project root). If the file is present, a gap analysis has already been run for this sprint — skip the tip entirely and proceed to Step 4 (the prompt is only shown when the file is absent). If the file does not exist, fall through to the display action below.</action>
  <action>DISPLAY TIP (AC1): Emit this exact one-line tip to the user: "Tip: Run `/gaia-test-gap-analysis` before selecting stories to identify untested areas." This wording is verbatim from FR-237 / AC1 and must not be paraphrased. The tip is informational — it does NOT gate sprint planning, and the scrum master may continue without running gap analysis.</action>
  <action>YOLO AUTO-CONTINUE (AC3): In YOLO mode, display the tip and auto-continue after 2 seconds without waiting for user input. In normal mode, display the tip and wait for the user to press continue via the ask below.</action>
  <ask if="not yolo_mode">Continue to story selection? [continue]</ask>
</step>
<step n="4" title="Story Selection">
  <action>Read sizing_map from {project-root}/_gaia/_config/global.yaml. Use these canonical point values for T-shirt sizes (S/M/L/XL → points). Display the mapping to the user before selection.</action>
  <action>Select stories for this sprint based on priority and dependencies — only from stories classified as SELECTABLE in Step 1 (status = 'ready-for-dev').</action>
  <action>Ensure total size fits within velocity estimate using the sizing_map point values</action>
  <action>Dependency blocking: for each candidate story, check its depends_on list. If any dependency story is NOT in 'done' status, the story CANNOT be included in this sprint. Display: "BLOCKED: Story {key} depends on {dep_key} (status: {dep_status}) — cannot be included until {dep_key} is done."</action>
  <action>Priority surfacing: after story selection, check for P0 stories that are 'ready-for-dev' but were NOT selected. If any found, display: "WARNING: The following P0/critical stories are ready but not selected for this sprint:" followed by the list with their sizes. Ask user to confirm they intentionally excluded these high-priority stories.</action>
  <action>If {test_artifacts}/test-plan.md exists: apply risk levels to story selection — buffer 20% for high-risk stories in velocity estimate</action>
  <action>ATDD check (high-risk ONLY): for each story with risk = high, check if {test_artifacts}/atdd-{story_key}.md exists. If missing, add a sprint risk note: "HIGH-RISK story {key} has no ATDD file — run /gaia-atdd {key} before development." Do NOT flag medium or low risk stories — ATDD is required only for high-risk stories.</action>
  <action>Auto-include flagged stories: after initial story selection, check if any SELECTABLE stories have priority_flag: "next-sprint" in their frontmatter but were NOT yet selected. For each such story: check remaining sprint capacity (velocity_capacity minus currently selected points). If the flagged story fits within remaining capacity, auto-include it in the sprint selection and inform the scrum master: "AUTO-INCLUDED: Story {key} — {title} (priority_flag: next-sprint, {points} pts). Flagged story auto-included — capacity allows." If the flagged story does NOT fit within remaining capacity, alert the scrum master for manual decision: "CAPACITY ALERT: Flagged story {key} — {title} ({points} pts) cannot be auto-included — insufficient capacity ({remaining} pts remaining). Include anyway (will exceed velocity) or defer to next sprint?"</action>
</step>
<step n="4b" title="Dependency Inversion Lint (E28-S33)">
  <critical>
    <mandate>This step detects forward references in acceptance criteria — when one story's AC mentions a resource that another story in the same sprint creates. Origin: E28-S4 finding #2 (E28-S1 AC3 referenced the gaia-public marketplace repo that E28-S4 creates but E28-S1 was scheduled first). Scope is AC-only: resource mentions in Dev Notes or Technical Notes do NOT trigger the lint. Knowledge: {project-root}/_gaia/lifecycle/knowledge/sprint-planning/dependency-inversion-lint.md.</mandate>
  </critical>
  <action>For each selected story, load its markdown file from {implementation_artifacts}/{story_key}-*.md. Build an array of {key, title, order, markdown} where `order` is the story's 1-indexed position in the sprint execution order produced by Step 4.</action>
  <action>Invoke the lint helper at {project-path}/scripts/lib/sprint-plan-dep-inversion-lint.js — call `lintSprintPlan(stories)` and then `formatWarnings(result)`. The helper is a pure ESM module with no I/O. Display the formatted output to the scrum master.</action>
  <action>If warnings were emitted: display each warning with the suggested re-ordering (move producer before consumer, OR add depends_on). This is ADVISORY — it does NOT halt sprint planning. The scrum master may accept the warning and re-run Step 4, add an explicit depends_on on the consumer story, or acknowledge and proceed.</action>
  <action>Record the lint result in the sprint plan output (Step 9) under a "Dependency Inversion Lint" heading — either "no issues found" or the list of warnings with suggested fixes.</action>
</step>
<step n="5" title="Update Story Files">
  <action>For each selected story that has an individual file in {implementation_artifacts}/: update the sprint_id field to "sprint-{N}"</action>
  <action>For each selected story: invoke status-sync protocol to assign the sprint — keep status as 'ready-for-dev' (do NOT change to backlog). Stories remain ready-for-dev until /gaia-dev-story transitions them to in-progress.
    <invoke-protocol ref="status-sync" story_key="{story_key}" new_status="ready-for-dev" sprint_id="sprint-{N}" source_workflow="sprint-planning" />
    Note: sprint-status.yaml may not exist yet at this point (created in Step 8) — the protocol will skip the sprint-status.yaml update if the file doesn't exist.</action>
  <action>Clear priority flag on sprint assignment: for each selected story that has priority_flag: "next-sprint" in its frontmatter, set priority_flag back to null. This prevents the flag from persisting after the story has been assigned to a sprint. The flag is a one-time scheduling hint — once the story is sprint-assigned, it is no longer needed.</action>
  <action>Include all modified story files in the checkpoint files_touched with sha256 checksums</action>
</step>
<step n="6" title="Optional: Mobile Testing">
  <ask>Would you like to create a mobile test plan? This spawns a subagent in a separate context. Recommended for mobile or responsive applications. (yes / skip)</ask>
  <action>If yes: spawn a subagent using the Agent tool: "Load {project-root}/_gaia/core/engine/workflow.xml, then process {project-root}/_gaia/testing/workflows/mobile-testing/workflow.yaml as workflow-config. Run in YOLO mode. Follow the workflow engine instructions EXACTLY."</action>
  <action>If skip: mobile testing can be set up anytime later with /gaia-mobile-testing</action>
</step>
<step n="7" title="Sprint Plan Generation">
  <action>Create sprint plan with story assignments and execution order</action>
  <action>Order by dependency resolution + priority</action>
  <action>Generate a Sprint Burndown Estimate table showing projected points remaining per day based on wave/story completion schedule. Format: Day | Points Remaining | Stories Completing. This helps the team track expected progress and identify deviations early.</action>
</step>
<step n="8" title="Status Initialization">
  <action>All selected stories remain in 'ready-for-dev' status (assigned to sprint but not yet started)</action>
  <action>Generate sprint-status.yaml with standardized schema:
    sprint_id: "sprint-{N}"
    duration: "{1 week / 2 weeks / custom}"
    velocity_capacity: {velocity estimate from Step 3}
    team_size: {number of developers}
    total_points: {sum of all selected story points}
    capacity_utilization: "{total_points / velocity_capacity * 100}%"
    started: "{date}"
    end_date: "{date}"
    stories:
      - key: "{story-key}"
        title: "{story title}"
        status: "ready-for-dev"
        points: {story points from sizing_map}
        risk_level: "{high/medium/low}"
        assignee: null
        blocked_by: null
        updated: "{date}"
  </action>
</step>
<step n="9" title="Generate Output">
  <template-output file="{implementation_artifacts}/{sprint_id}-plan.md">
    Generate sprint plan document with: sprint goals, selected stories (ordered), velocity target, risk assessment, Sprint Burndown Estimate (points remaining per day), and a Testing Readiness section listing ONLY high-risk stories with their ATDD file status. Medium and low risk stories do NOT require ATDD and should NOT appear in the ATDD readiness checks.
  </template-output>
  <action>Write sprint-status.yaml to {implementation_artifacts}/sprint-status.yaml</action>
</step>
<step n="10" title="Val Validation">
  <action>Invoke Val to validate the sprint plan. Val verifies:
    (a) All selected story keys exist as story files with status 'ready-for-dev'
    (b) Dependency ordering is correct — no story selected before its blocking dependency is 'done'
    (c) Points math is correct — total selected points ≤ velocity capacity
    (d) No duplicate story keys in the selection
    (e) Sprint-status.yaml entries match the sprint plan document
    If findings: SM auto-fixes (e.g., remove invalid stories, fix ordering), re-validates.
    If Val fails or crashes: log warning and continue — validation is non-blocking for sprint planning.</action>
  <invoke-workflow
    target="{project-root}/_gaia/lifecycle/workflows/4-implementation/val-validate-artifact/workflow.yaml"
    input_artifact="{implementation_artifacts}/{sprint_id}-plan.md"
    on_error="warn_and_continue"
    model_override="opus" />
</step>
<step n="11" title="Save to Val Memory">
  <action>Auto-save sprint planning decisions to Val's memory sidecar (no user prompt required):

  1. Append to {memory_path}/validator-sidecar/decision-log.md:
     ### [YYYY-MM-DD] Sprint Planning: {sprint_id}

     - **Agent:** validator
     - **Workflow:** sprint-planning
     - **Sprint:** {sprint_id}
     - **Status:** recorded

     Sprint {sprint_id} planned. Duration: {duration}. Velocity: {velocity_capacity} pts.
     Stories selected: {count} ({total_points} pts, {utilization}% capacity).
     Story keys: {comma-separated list}.
     High-risk stories: {count} (ATDD: {verified/missing}).
     Retro action items carried forward: {count or "none"}.

  2. Replace body of {memory_path}/validator-sidecar/conversation-context.md (preserve header above first ---):
     Last session: Sprint planning for {sprint_id}.
     Date: {YYYY-MM-DD}. Stories: {count} ({total_points} pts). Velocity: {velocity_capacity}.

  If {memory_path}/validator-sidecar/ directory or files don't exist, create them with standard headers.
  If writing fails, log warning and continue — memory save is non-blocking.</action>
</step>
<next-step source="lifecycle-sequence.yaml">
  <primary command="/gaia-dev-story">Start implementing the first story from the sprint</primary>
  <alternative command="/gaia-sprint-status" context="To review current sprint state"/>
</next-step>
</workflow>
