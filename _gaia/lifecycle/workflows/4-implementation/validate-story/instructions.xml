<workflow name="validate-story">
<critical>
  <mandate>Append validation results to the story file</mandate>
  <mandate>Read the story template from {project-root}/_gaia/lifecycle/templates/story-template.md for reference</mandate>
</critical>
<step n="1" title="Load Story">
  <ask>Which story to validate? (provide story key, e.g., 1.2)</ask>
  <action>Find and read the story file matching {story_key}-*.md in {implementation_artifacts}/</action>
</step>
<step n="2" title="YAML Frontmatter Validation">
  <action>Verify YAML frontmatter block exists (delimited by --- markers after the story title)</action>
  <action>Check ALL 14 required fields are present and populated:
    - key: must match the story key pattern (e.g., E1-S1)
    - title: must not be empty
    - epic: must reference a valid epic
    - status: must be one of [backlog, validating, ready-for-dev, in-progress, blocked, review, done]
    - priority: must be one of [P0, P1, P2]
    - size: must be one of [S, M, L, XL]
    - points: must be a number matching sizing_map (S→2, M→5, L→8, XL→13)
    - risk: must be one of [high, medium, low]
    - sprint_id: null or a valid sprint ID (e.g., sprint-1)
    - depends_on: list of story keys (can be empty [])
    - blocks: list of story keys (can be empty [])
    - traces_to: list of requirement IDs (can be empty [])
    - date: must be a valid date
    - author: must not be empty
  </action>
  <action>FAIL if any required field is missing or has an invalid value. Report each missing/invalid field individually.</action>
</step>
<step n="3" title="Semantic Quality Checks">
  <action>Duplicate detection: load all story files for the same epic from {implementation_artifacts}/. Compare titles and acceptance criteria scope for overlap. Flag any stories with >70% AC overlap as potential duplicates.</action>
  <action>Architecture cross-reference: load {planning_artifacts}/architecture.md. For each ADR referenced in the story, verify the ADR exists and its status is Accepted. Flag references to Proposed or Deprecated ADRs as warnings. Flag story approaches that conflict with architecture decisions as errors.</action>
  <action>Report findings even if clean: "No duplicate stories found in epic" / "All ADR references valid"</action>
</step>
<step n="4" title="Completeness">
  <action>Verify all sections present: User Story, Acceptance Criteria, Tasks/Subtasks, Dev Notes, Technical Notes, Dependencies, Test Scenarios, Project Structure Notes, References, Dev Agent Record, Estimate, Definition of Done</action>
</step>
<step n="5" title="Clarity">
  <action>Check each AC is testable and unambiguous (Given/When/Then format)</action>
  <action>Verify subtasks are actionable and linked to AC numbers</action>
  <action>Verify Definition of Done items are specific and measurable</action>
</step>
<step n="6" title="Dependencies">
  <action>Verify all declared dependencies exist and are resolvable</action>
</step>
<step n="7" title="Update Status and Generate Validation Report">
  <action>If overall verdict is PASS: update story status to 'ready-for-dev' in BOTH the YAML frontmatter AND the body status line</action>
  <action>If overall verdict is FAIL: update story status to 'validating' in BOTH the YAML frontmatter AND the body status line</action>
  <action>Append validation result at the END of the story file, after a horizontal rule (---) separator</action>
  <action>Also create a separate validation report file at {implementation_artifacts}/story-validation-{story_key}.md with YAML frontmatter:
    ---
    story_key: "{story_key}"
    validation_status: "PASS" or "FAIL"
    validated_at: "{ISO-8601 timestamp}"
    findings:
      - section: "{section name}"
        result: "PASS" or "FAIL"
        issue: "{description if FAIL}"
    ---
    Then detailed findings for each section.</action>
  <template-output file="{implementation_artifacts}/{story_key}-{story_title_slug}.md">
    Save the story file with status updated (ready-for-dev if PASS, validating if FAIL) in both YAML frontmatter and body, and validation report appended at the end after a --- separator. Include: date, overall PASS/FAIL verdict, and per-section findings. This report will be read by /gaia-fix-story to resolve issues.
  </template-output>
  <template-output file="{implementation_artifacts}/story-validation-{story_key}.md">
    Create standalone validation report with YAML frontmatter (story_key, validation_status, validated_at, findings array) and detailed per-section results. This file is the machine-readable validation artifact for downstream automation.
  </template-output>
  <action>If overall verdict is FAIL: instruct user to run /gaia-fix-story then re-run /gaia-validate-story</action>
</step>
<next-step source="lifecycle-sequence.yaml">
  <on_pass command="/gaia-create-story">Create more stories, or run /gaia-sprint-plan when all stories are ready</on_pass>
  <on_fail command="/gaia-fix-story">Fix validation failures, then re-validate</on_fail>
  <alternative command="/gaia-sprint-plan" context="When all stories are created and validated (ready-for-dev)"/>
</next-step>
</workflow>
