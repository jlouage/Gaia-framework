<workflow name="validate-story">
<critical>
  <mandate>Val performs full validation — completeness, clarity, semantics, dependencies, and factual verification</mandate>
  <mandate>Append validation results to the story file</mandate>
  <mandate>Read the story template from {project-root}/_gaia/lifecycle/templates/story-template.md for reference</mandate>
  <mandate>Auto-save all validation results to Val memory sidecar — no user prompt required</mandate>
</critical>
<step n="1" title="Load Story">
  <ask>Which story to validate? (provide story key, e.g., E1-S2)</ask>
  <action>Find and read the story file matching {story_key}-*.md in {implementation_artifacts}/</action>
  <action>Read the story template from {project-root}/_gaia/lifecycle/templates/story-template.md for reference</action>
  <action>Read {planning_artifacts}/architecture.md for cross-reference validation</action>
  <action>Read {planning_artifacts}/epics-and-stories.md for context</action>
  <action>Read {memory_path}/validator-sidecar/ground-truth.md if it exists (for factual verification)</action>
</step>
<step n="2" title="Val Full Validation">
  <action>Perform full validation on the story file:

  (a) YAML Frontmatter — verify all 14 required fields present and valid:
      key (pattern E{n}-S{n}), title (not empty), epic (valid reference),
      status (valid state), priority (P0/P1/P2), size (S/M/L/XL),
      points (matches sizing_map: S→2, M→5, L→8, XL→13), risk (high/medium/low),
      sprint_id (null or valid), depends_on (list), blocks (list),
      traces_to (list), date (valid date), author (not empty).
      FAIL if any required field is missing or invalid.

  (b) Completeness — verify all template sections present:
      User Story, Acceptance Criteria, Tasks/Subtasks, Dev Notes, Technical Notes,
      Dependencies, Test Scenarios, Project Structure Notes, References,
      Dev Agent Record, Estimate, Definition of Done.

  (c) Clarity — check each AC is testable and in Given/When/Then format,
      subtasks are actionable and linked to AC numbers,
      Definition of Done items are specific and measurable.

  (d) Semantic Quality — duplicate detection: load all story files for the same
      epic, flag stories with >70% AC overlap as potential duplicates.
      ADR cross-reference: verify each referenced ADR exists in architecture.md
      with Accepted status. Flag Proposed/Deprecated references as warnings.

  (e) Dependencies — verify all declared dependencies exist and resolve.

  (f) Factual Verification — extract verifiable claims from the story
      (file paths, component references, API endpoints, dependency versions).
      Verify against filesystem and ground truth (if available).
      Classify findings as CRITICAL (broken reference), WARNING (outdated), INFO (style).
  </action>
</step>
<step n="3" title="Validation Fix Loop">
  <check if="zero findings from Step 2">
    <invoke-protocol ref="status-sync" story_key="{story_key}" new_status="ready-for-dev" source_workflow="validate-story" />
    <action>Report "Validation passed — story is ready for dev." Proceed to Step 4.</action>
  </check>

  <check if="findings exist">
    <action>Set validation_attempt = 1.</action>
    <invoke-protocol ref="status-sync" story_key="{story_key}" new_status="validating" source_workflow="validate-story" />
    <action>Present findings table to user (severity, section, claim, finding, evidence).</action>
    <action>SM (Nate) addresses each finding:
      - For each CRITICAL or WARNING finding: fix the story content directly
      - For each INFO finding: fix or acknowledge with a note
      Present the corrected story sections to user for confirmation.</action>
    <action>After SM applies all fixes, write the updated story file. Increment validation_attempt.</action>
    <action>Re-run Val validation on the updated story (same checks as Step 2).
      - If zero findings: invoke status-sync with new_status='ready-for-dev'. Exit loop. Proceed to Step 4.
      - If findings remain AND validation_attempt &lt; 3: SM fixes again, Val re-validates.
      - If findings remain AND validation_attempt = 3 (hard limit): keep status as 'validating',
        report "Validation limit reached — {N} findings remain. Run /gaia-validate-story {story_key} to continue."
        Proceed to Step 4.</action>
  </check>
</step>
<step n="4" title="Generate Validation Report">
  <action>Append validation result at the END of the story file, after a horizontal rule (---) separator.
    Include: date, overall PASS/FAIL verdict, validation attempts count, and per-section findings.</action>
  <action>Create a separate validation report file at {implementation_artifacts}/story-validation-{story_key}.md with YAML frontmatter:
    ---
    story_key: "{story_key}"
    validation_status: "PASS" or "FAIL"
    validated_at: "{ISO-8601 timestamp}"
    validation_attempts: {N}
    findings:
      - section: "{section name}"
        severity: "CRITICAL" or "WARNING" or "INFO"
        result: "PASS" or "FIXED" or "FAIL"
        issue: "{description if FAIL or FIXED}"
    ---
    Then detailed findings for each section.</action>
  <template-output file="{implementation_artifacts}/{story_key}-{story_title_slug}.md">
    Save the story file with status updated (ready-for-dev if PASS, validating if FAIL) and validation report appended.
  </template-output>
  <template-output file="{implementation_artifacts}/story-validation-{story_key}.md">
    Create standalone validation report with YAML frontmatter and detailed per-section results.
  </template-output>
</step>
<step n="5" title="Save to Val Memory">
  <action>Auto-save all validation results to Val's memory sidecar (no user prompt required):

  1. Append to {memory_path}/validator-sidecar/decision-log.md using standardized format:
     ### [YYYY-MM-DD] Story Validation: {story_key}

     - **Agent:** validator
     - **Workflow:** validate-story
     - **Sprint:** {sprint_id or "N/A"}
     - **Status:** recorded
     - **Related:** {story_key}

     Validated story {story_key} ({story_title}).
     Result: {PASS or FAIL after N attempts}.
     Findings: {total_count} total, {fixed_count} fixed, {remaining_count} remaining.
     Final status: {ready-for-dev or validating}.
     {Brief summary of key findings and fixes}

  2. Replace body of {memory_path}/validator-sidecar/conversation-context.md (preserve header above first ---):
     Last session: Story validation of {story_key} ({story_title}).
     Date: {YYYY-MM-DD}. Result: {PASS/FAIL}. Attempts: {N}.
     Findings: {count}. Fixed: {fixed_count}.

  If _memory/validator-sidecar/ directory or files don't exist, create them with standard headers.
  If writing fails, log warning and continue — memory save is non-blocking.</action>
</step>
<next-step source="lifecycle-sequence.yaml">
  <on_pass command="/gaia-create-story">Create more stories, or run /gaia-sprint-plan when all stories are ready</on_pass>
  <on_fail command="/gaia-fix-story">Fix validation failures, then re-validate</on_fail>
  <alternative command="/gaia-sprint-plan" context="When all stories are created and validated (ready-for-dev)"/>
</next-step>
</workflow>
