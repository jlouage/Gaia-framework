<workflow name="create-story">
<critical>
  <mandate>Story files must include complete YAML frontmatter with ALL required fields: key, title, epic, status, priority, size, points, risk, sprint_id, depends_on, blocks, traces_to, date, author</mandate>
  <mandate>All acceptance criteria MUST use Given/When/Then format: "Given {context}, when {action}, then {expected result}"</mandate>
  <mandate>Check web access availability if story references external APIs</mandate>
</critical>
<step n="1" title="Select Story">
  <action>Read {planning_artifacts}/epics-and-stories.md — parse all stories with their keys, titles, priorities, sizes, risk levels, and dependencies.</action>
  <action>Scan {implementation_artifacts}/ for existing story files matching *-*.md patterns. Record which story keys already have individual files.</action>
  <action>Build a list of stories that do NOT yet have individual story files.</action>
  <action>Sort the list by: Priority (P0 first, then P1, then P2), then dependency topology (stories with no dependencies first).</action>
  <check if="user provided a story key (e.g., /gaia-create-story E1-S2)">
    Use the provided story key directly — skip the selection list. Verify the key exists in epics-and-stories.md. If a story file already exists for this key, warn the user: "Story file for {key} already exists. Proceeding will overwrite it."
  </check>
  <check if="no story key provided">
    Display the prioritized list of stories without files as a table:
    | # | Key | Title | Priority | Size | Risk | Depends On |
    Mark P0 stories with a ⚠ indicator.
    <ask>Which story do you want to create? Select from the list above (provide the story key).</ask>
  </check>
</step>
<step n="2" title="Load Context">
  <action>Read story summary from epics-and-stories.md</action>
  <action>Read architecture.md for technical context</action>
</step>
<step n="3" title="Web Access Guard">
  <check if="story_references_external_apis">Check if web tools available for API documentation lookup</check>
  <check if="no_web_access">Note limitation in story file</check>
</step>
<step n="4" title="Elaborate Story">
  <action>Present a brief summary of what was loaded from epics and architecture context for this story.</action>
  <ask>Are there specific edge cases or error scenarios to call out in the acceptance criteria?</ask>
  <ask>Any implementation preferences or constraints not in the architecture? (libraries, patterns, performance targets)</ask>
  <ask>Should any acceptance criteria be split out or deprioritized?</ask>
  <ask>Any additional context, stakeholder notes, or dependencies to include?</ask>
  <action>Incorporate user responses into the story elaboration</action>
  <action>Expand acceptance criteria in Given/When/Then format: "Given {context}, when {action}, then {expected result}"</action>
  <action>Add technical notes from architecture context</action>
  <action>Define subtasks for implementation</action>
</step>
<step n="5" title="Definition of Done">
  <action>Append DoD checklist: all AC met, tests pass, code reviewed, docs updated</action>
</step>
<step n="6" title="Generate Output">
  <action>Read the story template from {project-root}/_gaia/lifecycle/templates/story-template.md</action>
  <action>Read sizing_map from {project-root}/_gaia/_config/global.yaml to resolve T-shirt size to story points (S→2, M→5, L→8, XL→13).</action>
  <action>Populate ALL YAML frontmatter fields from epics-and-stories.md data:
    - key: story key from epics (e.g., E1-S1)
    - title: story title
    - epic: epic key and name (e.g., "E1 — Framework Core")
    - status: backlog (always — sprint assignment happens later in /gaia-sprint-plan)
    - priority: P0/P1/P2 from epics
    - size: S/M/L/XL from epics
    - points: resolved from sizing_map (e.g., L → 8)
    - risk: high/medium/low from epics or test-plan risk levels
    - sprint_id: null (assigned by /gaia-sprint-plan)
    - depends_on: list of story keys this story depends on (from epics)
    - blocks: list of story keys this story blocks (from epics)
    - traces_to: requirement IDs this story traces to (from epics)
    - date: current date
    - author: agent name (e.g., "Nate (Scrum Master)")
  </action>
  <template-output file="{implementation_artifacts}/{story_key}-{story_title_slug}.md">
    Generate story file following the story-template.md structure. The filename must use the story key and slugified title (e.g., E1-S1-user-login.md). Include complete YAML frontmatter with ALL 14 fields populated. Fill all template sections: User Story, Acceptance Criteria, Tasks/Subtasks (linked to AC numbers), Dev Notes, Technical Notes, Dependencies, Test Scenarios, Project Structure Notes, References, Dev Agent Record, and Estimate.
  </template-output>
</step>
<step n="7" title="Auto-Validate Story with Val">
  <action>Read {project-root}/_gaia/_config/global.yaml and check for val_integration field</action>
  <check if="val_integration is false OR val_integration field is absent">Skip auto-validation — val_integration is disabled or not configured. Proceed to next-step.</check>

  <action>Read the risk field from the newly created story file's YAML frontmatter</action>
  <check if="risk == 'low'">Skip auto-validation — low-risk stories bypass validation to avoid noise during batch creation. Proceed to next-step.</check>

  <action>Invoke Val validation as a subagent. Wrap the invocation in an error guard — if the subagent fails or crashes, log a warning and continue. The story file is already saved; auto-validation is an enhancement, not a gate.

  On success: check finding count from Val's response.
    — If zero findings AND YOLO mode: complete silently — no output, no pause, proceed to next-step.
    — If zero findings AND normal mode: report "Auto-validation complete — no findings." and proceed.
    — If non-zero findings (any mode): pause and present findings using the discussion → selective sharing loop:
      1. Display findings table (severity, section, claim, finding, evidence)
      2. For each finding, ask user: APPROVE (include in story), DISPUTE (exclude), or EDIT (modify before approving)
      3. Write approved findings to the story file under "## Validation Findings"
      4. If all findings disputed: report "All findings disputed — no changes written." and proceed.

  On failure: log warning "Auto-validation could not complete: {error_reason}" and proceed to next-step. Do NOT halt the workflow.</action>

  <invoke-workflow
    target="{project-root}/_gaia/lifecycle/workflows/4-implementation/val-validate-artifact/workflow.yaml"
    input_artifact="{implementation_artifacts}/{story_key}-{story_title_slug}.md"
    on_error="warn_and_continue"
    model_override="opus" />
</step>
<next-step source="lifecycle-sequence.yaml">
  <primary command="/gaia-validate-story">Validate the story file for completeness and quality</primary>
  <alternative command="/gaia-create-story" context="To create another story before validating"/>
</next-step>
</workflow>
